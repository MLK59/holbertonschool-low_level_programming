#include "main.h"
/**
 * _strlen - check the code of the function
 * @strng: paramter for the function _strlen
 *
 * Return 0.
 */
int _strlen(char *strng)
{
	int c;

	for (c = 0; strng[c] != 0; c++)
	{
		}
		return (c);
}
